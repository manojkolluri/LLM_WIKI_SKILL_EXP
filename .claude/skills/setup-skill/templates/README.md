# {{PROJECT_NAME}}

{{PROJECT_SUMMARY}}

## Repo layout

- `Wiki/` — intelligence layer (architecture, conventions, inventories, changelog). Read this first.
- `web/` — production code (placeholder; populate once the stack is chosen).
- `Examples/` — reference snippets used as canonical examples when building production code.
- `Experiments/` — prototypes iterated on before integration into production.
- `CLAUDE.md` — agent instructions and the required workflow for every change.

## Getting started

_Fill in once the stack is set up — install commands, dev server, env vars, etc._
